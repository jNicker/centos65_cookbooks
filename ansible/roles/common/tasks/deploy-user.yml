---

- name: Add Deployment User
  user: name={{ user }} password={{ password }} groups='wheel'

- name: Enable passwordless sudo for User
  template:
    src: passwordless-sudo.j2
    dest: '/etc/sudoers.d/{{ user }}'
    mode: 0440

